[package]
name = "json2jq"
version = "1.0.1"
edition = "2021"
description = "Converts a json string into jq paths"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.0", features = ["derive"] }
